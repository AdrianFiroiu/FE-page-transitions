# [FE]-Barba-Page-Transitions
Smooth page transitions using Barba.js - **PoC with local library injection**

Simply:
- run with SASS watcher
- OR add pachages with `npm` and `parcel` / `webpack` for a more robust setup

---